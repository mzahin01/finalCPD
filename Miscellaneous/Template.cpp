#include <bits/stdc++.h>
using namespace std;

#define ff first
#define ss second
#define fastio ios::sync_with_stdio(false); cin.tie(NULL)
#define fr(v,p,q) for(int v=p;p<=q?v<=q:v>=q;p<=q?v+=1:v-=1)
#define p(xd) for(auto it=begin(xd);it!=end(xd);it=next(it)){cout<<*it<<" ";}nl
#define sci(x) scanf("%d", &x)
#define linf (1ll << 62)
#define inf (1 << 30)
#define yes cout << "YES\n"
#define no cout << "NO\n"
#define nl cout << "\n"
#define UNIQUE(a) (a).erase(unique(all(a)),(a).end())
#define all(p) (p).begin(),(p).end()
#define ll long long
#define pb push_back
#define mp make_pair

int main() {
    fastio;
    int t;
    cin >> t;
    while (t--) {
        int n;
        cin >> n;


    }
}